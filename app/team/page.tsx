import Link from 'next/link'
import { ArrowLeft, ArrowUpRight, GitBranch, BriefcaseBusiness, Sparkles } from 'lucide-react'
import { mockTeam } from '../../data/findoc'
export default function TeamPage() { return <main className="simple-public-page"><header className="simple-public-nav"><Link href="/" className="marketing-brand"><span className="brand-mark"><Sparkles size={15}/></span>finmaester</Link><Link href="/" className="back-link"><ArrowLeft size={15}/> Back home</Link></header><section className="people-hero"><div className="section-eyebrow">THE PEOPLE BEHIND FINMAESTER</div><h1>Meet the team<br/><em>building clarity.</em></h1><p>FinMaester is an early-stage project. Replace these structured placeholders with your real team information as the product grows.</p></section><section className="people-grid">{mockTeam.map((person) => <article className="person-card" key={person.name}><div className="person-avatar">{person.name.slice(0,2).toUpperCase()}</div><div className="person-role">{person.role}</div><h2>{person.name}</h2><p>{person.bio}</p><div className="person-links">{person.links.map((link) => <a href="#" key={link}>{link === 'GitHub' ? <GitBranch size={15}/> : <BriefcaseBusiness size={15}/>} {link}<ArrowUpRight size={12}/></a>)}</div></article>)}</section></main> }
