import Link from 'next/link'
import { ArrowLeft, ArrowUpRight, GitBranch, BriefcaseBusiness, Sparkles } from 'lucide-react'
import { mockContributors } from '../../data/findoc'
export default function ContributorsPage() { return <main className="simple-public-page"><header className="simple-public-nav"><Link href="/" className="marketing-brand"><span className="brand-mark"><Sparkles size={15}/></span>finmaester</Link><Link href="/" className="back-link"><ArrowLeft size={15}/> Back home</Link></header><section className="people-hero"><div className="section-eyebrow">OPEN COLLABORATION</div><h1>Built by people<br/><em>who care about banking.</em></h1><p>Contributors help shape the way FinMaester explains the systems behind financial services. Every name and role below is ready to be replaced with real project data.</p></section><section className="people-grid contributors-grid">{mockContributors.map((person) => <article className="person-card" key={person.name}><div className="person-avatar">{person.name.slice(0,2).toUpperCase()}</div><div className="person-role">{person.contribution}</div><h2>{person.name}</h2><p>{person.bio}</p><div className="person-links">{person.links.map((link) => <a href="#" key={link}>{link === 'GitHub' ? <GitBranch size={15}/> : <BriefcaseBusiness size={15}/>} {link}<ArrowUpRight size={12}/></a>)}</div></article>)}</section></main> }
